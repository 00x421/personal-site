feat(identity): 名字改为「烦恼全無先生」，并恢复轨道提示文字

两件事。

一、名字：Linling Qi -> 烦恼全無先生
名字散落在 7 处，其中 4 处是硬编码字符串（不走 siteIdentity），
本次一并收拢：

- lib/site-content.ts                      name 字段（唯一真源）
- app/layout.tsx                           JSON-LD Person.name
- app/articles/[slug]/page.tsx             3 处：openGraph.authors /
                                           Article.author / Article.publisher

文章页的 3 处改为引用 siteIdentity.name，以后改名只需动一处。
（页面文案、肖像图 alt 本来就引用 siteIdentity，自动跟随。）

二、恢复「横向浏览更多文章」
上一轮我把它改成条件渲染（只在有整张卡被遮住时显示）。用户认为保留更好，
改回常显。

回头看那个条件判断确实是过度设计：为「桌面端 4 篇文章都露着一部分」
这个暂时状态，写了 20 行判断、跨 6 个断点验证，换来的只是少一行小字。
而且那句话本身没说错——轨道确实可以横向拖，它是操作提示，不是内容承诺。

顺带删掉窄屏的 .rail-footer > span { display: none }：实测没必要，
提示 94px + 间距 18px + 按钮 74px = 186px，360px 视口下容器宽 335px，
余量 149px。现在提示在所有宽度常显。

.rail-footer > div 的 margin-left: auto 保留——窄屏规则删掉后它不是必需的，
但比 justify-content: space-between 更稳（提示若将来再加条件渲染也不会甩按钮）。

验证：线上首页与文章页 HTML 均无 Linling Qi 残留、JSON-LD author 为
「烦恼全無先生」；提示在 1440 与 390 下均 display: block 且放得下；
npm test 76/76、tsc 零错误、oxlint 零告警。
